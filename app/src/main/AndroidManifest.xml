<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.utstream">

    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.UTStream">
        <activity android:name=".ui.CapitulosActivity"></activity>
        <activity android:name=".ui.BusquedaActivity" />
        <activity
            android:name=".ui.splash"
            android:configChanges="orientation|screenSize"
            android:theme="@style/SplashScreen">
            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.show"
            android:screenOrientation="landscape" />
        <activity android:name=".ui.MovieDetailActivity" />
        <activity android:name=".ui.MainActivity" />
    </application>

</manifest>